import { elementType, OnlyDrawElement } from '@/types/type';

type args = {
  elementType: elementType;
};
export const checkElement = ({ elementType }: args): OnlyDrawElement => {
  switch (elementType) {
    case value:
      break;

    default:
      break;
  }
};
